import { LatestRepoDirective } from './latest-repo.directive';

describe('LatestRepoDirective', () => {
  it('should create an instance', () => {
    const directive = new LatestRepoDirective();
    expect(directive).toBeTruthy();
  });
});
